from django.shortcuts import render

# Create your views here.
from django.shortcuts import render
from .models import WhiteCard, BlackCard

def get_cards(request):
    # Logic to get cards for the game
    pass